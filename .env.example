# Copy to .dev.vars for the local Cloudflare runtime, not to the browser.
# Generate with npm run demo:setup; never reuse this placeholder.
BOOTSTRAP_TOKEN=REPLACE_WITH_CRYPTOGRAPHIC_RANDOM_VALUE
SITE_HOST=inchouf-orderpilot.copper-goat-7392.chatgpt.site
# Durable DB and FILES bindings are declared in .openai/hosting.json.
# There is no payment, courier, messaging or external AI key.
